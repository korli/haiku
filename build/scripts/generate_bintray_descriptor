#!/bin/sh -vx

targetArch=x86_64

if [ -n "${BINTRAY-API-KEY}" ]; then

echo "{
    \"package\": { 
        \"name\": \"haiku.images\",
        \"repo\": \"haiku_images\",
        \"subject\": \"korli\"
    },
    \"version\": {
        \"name\": \"hrev99999\",
        \"released\": \"2016-08-19\"
    },
    \"files\":
        [
        {\"includePattern\": \"generated/(haiku.*)\", \"uploadPattern\": \"$TRAVIS_COMMIT/$1\"}
        ],
    \"publish\": true
}" > generated/bintray.json

fi
